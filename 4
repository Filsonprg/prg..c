#include <stdio.h>

int main() {


int score = 90;
    
if (score >= 90)
  printf ("%s", "TOP 10%\n");
else if (score >= 80)
  printf ("%s", "TOP 20%\n");
else if (score >= 75)
  printf ("%s", "PROŠEL JSI.\n");
else
  printf ("%s", "NEPROŠEL JSI.\n");
   return 0;
}
